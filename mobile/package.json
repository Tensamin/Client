{
  "name": "tensamin",
  "scripts": {
    "dev:desktop": "tauri dev",
    "dev:android": "tauri android dev",
    "dev:ios": "tauri ios dev",
    "build:desktop": "NO_STRIP=true tauri build",
    "build:android": "tauri android build",
    "build:ios": "tauri ios build",
    "install:android": "adb install ./gen/android/app/build/outputs/apk/universal/release/app-universal-release.apk"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.9.6"
  },
  "dependencies": {
    "@tauri-apps/plugin-deep-link": "~2"
  }
}
